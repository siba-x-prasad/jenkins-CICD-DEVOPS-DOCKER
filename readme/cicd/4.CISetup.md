# 🚀 Setting Up Continuous Integration (CI) with Jenkins — Step by Step Guide

## 🧩 What is Continuous Integration (CI)?

**Continuous Integration (CI)** is the process of automatically building and testing your code every time you or your team commits changes.

With CI, tools like **Jenkins** automatically:
1. Detect new code in your Git repository
2. Pull and build it
3. Run tests
4. Notify you if anything fails

This ensures your code always works correctly and integrates smoothly with others’ changes.

---

## 🖥️ Step 1: Install Jenkins

### ✅ Prerequisites
Make sure your system has:
- **Java 11 or newer**
- **Git** installed
- **Internet access**

---

### 🪟 Windows Installation
1. Go to [https://www.jenkins.io/download](https://www.jenkins.io/download)
2. Download the **Windows installer**
3. Run the installer (it installs Jenkins as a service)
4. Open your browser and visit:  
   👉 `http://localhost:8080`

---

### 🐧 Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install openjdk-11-jdk
curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io.key | sudo tee \
  /usr/share/keyrings/jenkins-keyring.asc > /dev/null
echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update
sudo apt install jenkins
sudo systemctl start jenkins
sudo systemctl enable jenkins
```
- Open your browser:
👉 http://localhost:8080

## 🍎 macOS (Homebrew)
```
brew install jenkins-lts
brew services start jenkins-lts
```
- Then visit: http://localhost:8080

## 🔐 Step 2: Unlock Jenkins
- Jenkins will display an unlock page asking for an admin password.
- Run this command to find it:
```
sudo cat /var/lib/jenkins/secrets/initialAdminPassword
```

- Copy and paste the password in your browser.
- Choose “Install Suggested Plugins”
- Create an admin user and finish setup.
- 🎉 Jenkins is ready to use!

## ⚙️ Step 3: Configure Jenkins
- Install the Git Plugin
- Go to Manage Jenkins → Plugins → Available plugins
- Search “Git Plugin”
- Install and restart Jenkins
- Configure Git
- Go to Manage Jenkins → Global Tool Configuration
- Under “Git”, add:
```
Name: Git
Path: /usr/bin/git
```
## 🔗 Step 4: Connect Jenkins to GitHub
- Create a GitHub Repository
- Create a new repo (e.g. my-sample-app) and push your code.
- Add a GitHub Webhook (Optional but Recommended)
- In your GitHub repo, go to Settings → Webhooks → Add Webhook
- Payload URL: 
- http://<your-jenkins-server>:8080/github-webhook/
- Content type: application/json
- Select “Just the push event”
- Save
- Now GitHub will notify Jenkins when you push new code.

## 🏗️ Step 5: Create Your First Jenkins Job (Freestyle CI)
- Create a New Job
- Click “New Item”
- Name it: MyApp-CI
- Choose Freestyle Project
- Click OK
- Configure the Job
- Source Code Management:
- Select Git
- Enter your repository URL
(e.g. https://github.com/yourusername/my-sample-app.git)
- Add credentials if it’s private.
- Build Triggers:
- Check “GitHub hook trigger for GITScm polling”

### Build Steps:
- Add a build step → “Execute shell” and enter:
```
echo "Building the project..."
mvn clean install   # For Java (Maven)
```
# or npm install && npm test  # For Node.js
- Post-Build Actions (Optional):
- Archive artifacts
- Send email notification
- Click Save

## Step 6: Run Your CI Job
- You can trigger builds in two ways:
- Manual: Click “Build Now”
- Automatic: Push new code to GitHub — Jenkins will start the job automatically!
- Check:
- Console output for real-time logs
- Status (✅ Success or ❌ Failure)
- Artifacts generated (e.g. .jar file)

## Step 7: Verify Continuous Integration
- Now every time you push new code:
- Jenkins automatically detects the change
- Pulls latest code
- Builds it
- Runs tests
- Reports the results

🎉 You now have Continuous Integration set up!

## Example CI Workflow (Visual Overview)
```
Developer Commit
↓
GitHub Webhook
↓
[Jenkins]
→ Pull Code
→ Build (mvn / npm)
→ Run Tests
→ Archive Results
↓
Report Success/Failure
```
##  Next Steps
- Once CI is working, you can extend your pipeline to include:
- Continuous Delivery (CD): Automatically deploy to a staging environment
- Continuous Deployment (CD): Automatically deploy to production
- Notifications: Slack or email integration for build updates
- Docker Integration: Build container images in Jenkins
- Pipeline as Code: Use a Jenkinsfile for more flexibility

## Helpful Resources
- 🔗 Jenkins Official Documentation
- 🐙 GitHub Webhooks Guide
- 🧱 Jenkins Pipeline Tutorial
- 💡 Understanding CI/CD

## 🏁 Summary
Step	Description
1️⃣ Install Jenkins	Get Jenkins running locally or on a server
2️⃣ Unlock Jenkins	Complete initial setup and admin configuration
3️⃣ Configure Tools	Add Git and necessary plugins
4️⃣ Connect GitHub	Link Jenkins with your repository
5️⃣ Create Job	Define how Jenkins should build your project
6️⃣ Run CI	Build automatically on every code push